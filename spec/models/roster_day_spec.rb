require 'spec_helper'

describe RosterDay do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    RosterDay.create!(@valid_attributes)
  end
end
